# SPEC.md — koho-lens(広報レンズ)

<!-- scaffold template v1.8.0 から展開(2026-08-20)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

日本の主要 IT サービス企業 11 社(富士通・NEC・日立製作所・NTTデータ・NRI・SCSK・TIS・
アクセンチュア・日本IBM・日鉄ソリューションズ・キヤノンITソリューションズ)の最新プレス
リリースを 1 ページで一望する。各社の公式発表を「ヘッドライン+リンク」で 5 件ずつ並べ、
定時自動更新される静的サイトとして公開する(hodo-hangenki 方式: Actions cron → commit →
Vercel Git 連携配信)。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 11 社それぞれの公式プレスリリース一覧から最新 5 件(タイトル・絶対 URL・日付)を取得できる(供給が 5 件未満のサイトは取得できた件数) | must |
| F-02 | 取得結果を `data/releases.json` に保存する(スキーマは §4 G-02) | must |
| F-03 | `out/index.html` に 11 社 × 最新 5 件をヘッドライン(リンク付き)で一覧表示する。会社名・日付・取得時刻(JST)・出典を明記 | must |
| F-04 | GitHub Actions cron(6 時間間隔)で 取得 → 再レンダリング → コミットし、Vercel が自動配信する | must |
| F-05 | ある会社の取得が失敗しても他社の更新は続行し、失敗した会社は前回取得分を保持する(グレースフル劣化) | must |
| F-06 | 各社の取得経路(フィード or HTML)は `src/sources.py` の宣言的定義に集約し、追加・修正が 1 箇所で済む | should |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | ランタイム依存は Python 3.11+ 標準ライブラリのみ(pytest は dev のみ) | CI で `pip install` なしに fetch/render が動く |
| N-02 | 法務規約: 保存するのは見出し・リンク・日付のみ(本文非保存)。収集は 6 時間間隔。UA にプロジェクト URL を明記。出典(会社名・公式サイト)をページに表示 | コードレビュー + T-201 |
| N-03 | 1 社の障害(タイムアウト・構造変更・bot ブロック)がジョブ全体を失敗させない | T-105 |
| N-04 | レンダリングは決定論(同じ JSON → バイト同一の HTML。埋め込む時刻は JSON 由来) | T-203 |

## 4. 品質基準

- G-01: フィクスチャ(実サイトの保存 HTML/フィード)に対し、11 社すべてのパーサが 3 件以上を抽出し、
  タイトル非空・URL 絶対・日付 ISO 形式を満たす
- G-02: `data/releases.json` スキーマ — `{generated_at: ISO8601Z, companies: [{id, name, source_url,
  fetched_at, ok: bool, items: [{title, url, date}]}]}`。companies は常に 11 要素・定義順
- G-03: 各 item.url は https で始まり、当該会社の許可ドメインリストに属する
- G-04: fetch は一部失敗でも exit 0(全社失敗のみ exit 1)。失敗会社は前回 items を引き継ぎ ok=false
- G-05: HTML 出力に 11 社の見出しリンクが全て `<a href>` として現れ、リンク総数 = Σ items(≤55)

## 5. スコープ外

- 本文の保存・全文検索・翻訳・要約(見出しとリンクのみ扱う)
- 過去リリースのアーカイブ蓄積・時系列解析(最新 5 件のスナップショットのみ)
- JS レンダリング必須サイトへのヘッドレスブラウザ投入(取得不能なら代替経路を検討し、無ければ ok=false 表示で運用)
- 5 件を超える件数表示・ページネーション
